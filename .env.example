# BeanCount environment variables
# Copy this to .env and fill in values (or run install.sh to auto-generate)

# Server port (default: 5000)
PORT=5000

# Session secret for cookie signing (auto-generated by install.sh)
# On first boot without .env, a development default is used.
SESSION_SECRET=
